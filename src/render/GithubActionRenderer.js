import { DefaultRender } from 'leto-modelizer-plugin-core';

/**
 * Class to render GithubAction files from components.
 */
class GithubActionRenderer extends DefaultRender {
  /**
   * Convert all provided components and links in GithubAction files.
   * @return {FileInput[]} - Array of generated files from components and links.
   */
  // eslint-disable-next-line class-methods-use-this
  render() {
    return [];
  }
}

export default GithubActionRenderer;
